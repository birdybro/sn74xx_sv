# ttl_74x591 — 8-bit binary counter with output register (OC)

Same logic as 74x590 but with open-collector outputs (modeled here as a
clean active driver since OC behavior is not synthesizable as internal logic).
