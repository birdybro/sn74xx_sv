# ttl_74x878 — Dual 4-bit D flip-flop with synchronous clear (3-state)

Like [ttl_74x874](../ttl_74x874/README.md), but `/SCLR` is sampled on the
rising edge of `clk` rather than acting asynchronously.
