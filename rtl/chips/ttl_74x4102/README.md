# ttl_74x4102 — 2-digit BCD presettable synchronous down counter

Two BCD digits (tens, ones) with synchronous parallel load (`load_n`), count
enable (`ce_n`), async reset (`rst`), and active-low terminal-count output
(`tc_n`, asserted when count reaches 0).
