# ttl_74x122 — Retriggerable monostable multivibrator (digital approximation)

Like `ttl_74x121` with retriggering and async clear. Digital approximation
of R-C-set pulse width via a `PULSE_CYCLES` parameter.
