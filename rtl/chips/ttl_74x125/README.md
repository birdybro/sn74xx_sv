# ttl_74x125 — Quad bus buffer, negative (active-low) enable, 3-state

Four buffers with independent active-low enables. When `oe_n[i]=0` the
buffer passes; when high, the output is 0 (muxed-zero convention).
