# ttl_74x800 — Triple 4-input AND/NAND drivers

Three independent 4-input AND gates, each providing both the AND
(active-high `y`) and NAND (active-low `y_n`) outputs on dedicated pins.
The 'AS800 is a high-drive variant; the drive-strength specs are not
modeled in RTL.
