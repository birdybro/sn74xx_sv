# ttl_74x173 — Quad D FF with enable and 3-state output

Async clear (active high). Two active-low enables (both must be low to
load on posedge clock). Async output enable `oe_n` gates the output
(muxed-zero convention).
