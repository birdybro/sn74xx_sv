# ttl_74x4015 — Dual 4-bit shift register

Two independent 4-bit serial-in / parallel-out shift registers, each with its
own clock, active-high async clear, and serial input. New bits enter at `q[0]`
and propagate toward `q[3]`.
