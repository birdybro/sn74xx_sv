# ttl_74x8161 — 8-bit synchronous binary counter

8-bit version of the 74x163 family: sync clear (`clr_n`), sync load
(`load_n`), dual count enables (`en_p` and `en_t`), and ripple-carry out
(`rco`) when count = 0xFF with `en_t` high.
