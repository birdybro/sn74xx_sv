# ttl_74x88 — 256-bit ROM (32x8)

Mask-programmable ROM. Async read. `INIT_FILE` parameter supplies a
`$readmemh` file with the contents; default is all zeros.

The real chip's open-collector outputs are not modeled.
