# ttl_74x4543 — BCD to 7-segment latch/decoder/driver for LCDs

Same decoder/latch as [ttl_74x4511](../ttl_74x4511/README.md), plus a phase
input (`ph`) that is XORed with every segment to support LCD AC drive.
