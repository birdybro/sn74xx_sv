# ttl_74x351 — Dual 8-to-1 mux with 4 common data inputs

Two 8-to-1 muxes share their lower 4 data inputs (`d_common`). Each has
its own upper 4 inputs (`d0_extra`, `d1_extra`) and active-low enable.
