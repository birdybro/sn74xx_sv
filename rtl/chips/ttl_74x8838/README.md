# ttl_74x8838 — 64-bit barrel shifter

Combinational 64-bit rotate: shifts `a` left or right by `shft` bits with
wrap-around (the chip's actual modes — logical/arithmetic shift, masked
zero-fill — are not modeled).
