# ttl_74x320 — Crystal-controlled oscillator (digital approximation)

Marked `digital_only_approximation`. The chip's analog crystal-drive
behavior cannot be modeled in RTL. Use the FPGA's MMCM/PLL for clock
generation.
