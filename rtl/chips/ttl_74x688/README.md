# ttl_74x688 — 8-bit identity comparator with /G enable (totem-pole)

Same equality test as the 682-family, but only `/P=Q` is provided (no
greater-than output) and a single active-low enable `g_n` gates the
output.
