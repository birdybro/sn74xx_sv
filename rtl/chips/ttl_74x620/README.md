# ttl_74x620 — octal bus transceiver, inverting outputs

Two independent enables (`gab` for A→B, `gba_n` for B→A) so both halves may be
active simultaneously (separate physical drivers in the real chip).
