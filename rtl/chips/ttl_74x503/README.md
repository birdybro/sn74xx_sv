# ttl_74x503 — 8-bit SAR with expansion control

Like the 74x502 but adds `cs_n` (chip select for cascading) and an active-low
`cco_n` (conversion-complete output) for cascading to higher-order SARs.
