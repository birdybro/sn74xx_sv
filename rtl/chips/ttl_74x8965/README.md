# ttl_74x8965 — 9-bit bidirectional latched FutureBus transceiver, latch select

Adds a per-direction `sel` input that chooses between real-time pass-through
and the latched value (analogous to the SAB/SBA control of 74x646).
