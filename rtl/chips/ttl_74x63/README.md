# ttl_74x63 — Hex current-sensing interface gates

Logical pass-through (`y = a`). The original part has special
current-sensing input characteristics for interfacing with non-TTL
signals; that electrical behavior is not modeled.
