# ttl_74x518 — 8-bit identity comparator

Active-high `p_eq_q` when both 8-bit inputs match and `g_n` is asserted.
The real part has an open-collector output; this FPGA model uses a clean
active-high level.
